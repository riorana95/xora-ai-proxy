{
  "name": "corestack-ai-proxy",
  "version": "1.1.0",
  "description": "AI proxy for CoreStack Interview Prep. Supports Cloudflare Workers AI (free) and Z.ai. Returns structured JSON the Angular frontend can consume.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5"
  },
  "optionalDependencies": {
    "z-ai-web-dev-sdk": "^0.0.18"
  }
}
